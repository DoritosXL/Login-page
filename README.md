# Base login screen with the following attributes

# Mandatory:
onClick (Action which the button should do, sign in for example)

Header: string (header text)
# Optional:
ImageUrl: string (URL to your preferred background image)

subText: string 
